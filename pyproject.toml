[tool.vercel]
entrypoint = "server:app"

[project]
name = "sb-dashboard"
version = "2.0.0"
description = "SB Group Executive Command Platform"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "pillow>=10.0.0",
]
